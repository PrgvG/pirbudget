/**
 * Домен: поступления (доходы).
 */

export * from './types';
export {
  fetchIncomeEntries,
  fetchIncomeEntry,
  createIncomeEntry,
  updateIncomeEntry,
  deleteIncomeEntry,
  isIncomeEntry,
  isIncomeEntryArray,
} from './api';
